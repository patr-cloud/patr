use crate::imports::*;

#[component]
pub fn CheckboxDropdown(
	/// The Additional classes for the outer div
	_class: MaybeSignal<String>,
	/// Placeholder for the input
	_placeholder: MaybeSignal<String>,
) -> impl IntoView {
	view! {}
}
