mod alignment;
mod color;
mod size;
mod variant;

pub use self::{alignment::*, color::*, size::*, variant::*};

pub mod constants {
	pub const FEATHER_IMG: &str = "/icons/sprite/feather-sprite.svg";
}
