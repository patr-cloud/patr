kind: pipeline
name: default

concurrency:
  limit: 1

steps:
  - name: "Install dependencies"
    image: rust:1
    commands:
      - cargo build
    when:
      event:
        - push
      branch:
        - master
        - staging
        - develop

trigger:
  event:
    - push
  branch:
    - master
    - staging
    - develop
